import React from 'react';
import PropTypes from 'prop-types';

Home.propTypes = {
    
};

function Home(props) {
    return (
        <div className="home">
            c
        </div>
    );
}

export default Home;